import MultiSetUI
import SwiftUI

/// The 3D model sandbox from the build prompt's §7.6.
///
/// Deliberately not implemented yet: it needs 4–6 licensed USDZ models chosen to
/// demonstrate occlusion, reflectivity, and real-world scale, and the asset brief
/// is explicit that text-to-3D output is not production quality for AR. Sourcing
/// those with recorded licences is the blocking step, not the code.
///
/// Tracked as Phase 6 work in ARCHITECTURE.md.
enum ShowcasePlaceholder {}
